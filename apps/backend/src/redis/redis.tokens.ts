/**
 * Токен внедрения клиента Redis.
 *
 * Вынесен в отдельный файл, чтобы разорвать циклический импорт между
 * redis.module.ts (импортирует CacheService) и cache.service.ts (импортирует
 * токен). При цикле токен оказывался undefined в момент выполнения декоратора
 * @Inject, и Nest не мог разрешить зависимость.
 */
export const REDIS = Symbol('REDIS');
